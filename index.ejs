<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube Thumbnail Downloader</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container py-5">
        <h1 class="text-center mb-4">YouTube Thumbnail Downloader</h1>
        <div class="card p-4 shadow">
            <form action="/download" method="POST">
                <div class="mb-3">
                    <label for="videoLink" class="form-label">Enter YouTube Video Link</label>
                    <input type="url" id="videoLink" name="videoLink" class="form-control" placeholder="https://www.youtube.com/watch?v=example" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Get Thumbnails</button>
            </form>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger mt-3"><%= error %></div>
        <% } %>

        <% if (videoId) { %>
            <div class="mt-5">
                <h3 class="text-center mb-3">Download Thumbnails for Video ID: <%= videoId %></h3>
                <div class="row g-3">
                    <% ["maxresdefault", "sddefault", "hqdefault", "mqdefault", "default"].forEach(size => { %>
                        <div class="col-md-4 text-center">
                            <img src="https://img.youtube.com/vi/<%= videoId %>/<%= size %>.jpg" alt="<%= size %> Thumbnail" class="img-fluid mb-2">
                            <a href="https://img.youtube.com/vi/<%= videoId %>/<%= size %>.jpg" target="_blank" class="btn btn-success w-100">Download <%= size.replace("default", " Default") %></a>
                        </div>
                    <% }); %>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>
